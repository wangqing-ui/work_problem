eng8000迁移报错修改SQL

CREATE OR REPLACE VIEW eng8000."11" AS select `status`.`iedid` as `iedid` , `bay`.`description` as `jg` , `ied`.`description` as `ied-des` , `status`.`description` as `stdescription` , `status`.`id` as `id` from `status` , `ied` , `bay` where `status`.`iedid` = `ied`.`id` ; CREATE OR REPLACE VIEW eng8000.yx AS select `bay`.`name` as `name` , `status`.`description` as `description` , `status`.`iedid` as `iedid` , `bay`.`id` as `id` from `bay` join `status` on `bay`.`id` = `status`.`iedid` ;

ics8000报错忽略视图

CREATE OR REPLACE VIEW ics8000.alarmview_2023 AS select `a`.`alarmtime` as `alarmtime` , `a`.`emittime` as `emittime` , `a`.`typeid` as `typeid` , `a`.`tasketypeid` as `tasketypeid` , `a`.`stationid` as `stationid` , `a`.`bayid` as `bayid` , `a`.`objtype` as `objtype` , `a`.`objid` as `objid` , `a`.`objname` as `objname` , `a`.`level` as `level` , `a`.`action` as `action` , `a`.`value` as `value` , `a`.`description` as `description` , `a`.`iedid` as `iedid` , `b`.`objectid` as `objectid` , `b`.`atime` as `atime` , `b`.`acktag` as `acktag` , `b`.`username` as `username` , `b`.`acktime` as `acktime` from `ics8000`.`alarm_2023` `a` left join `ics8000`.`alarmack_2023` `b` on `a`.`objid` = `b`.`objectid` and `a`.`alarmtime` = `b`.`atime` ; CREATE OR REPLACE VIEW ics8000.alarmview_2024 AS select `a`.`alarmtime` as `alarmtime` , `a`.`emittime` as `emittime` , `a`.`typeid` as `typeid` , `a`.`tasketypeid` as `tasketypeid` , `a`.`stationid` as `stationid` , `a`.`bayid` as `bayid` , `a`.`objtype` as `objtype` , `a`.`objid` as `objid` , `a`.`objname` as `objname` , `a`.`level` as `level` , `a`.`action` as `action` , `a`.`value` as `value` , `a`.`description` as `description` , `a`.`iedid` as `iedid` , `b`.`objectid` as `objectid` , `b`.`atime` as `atime` , `b`.`acktag` as `acktag` , `b`.`username` as `username` , `b`.`acktime` as `acktime` from `ics8000`.`alarm_2024` `a` left join `ics8000`.`alarmack_2024` `b` on `a`.`objid` = `b`.`objectid` and `a`.`alarmtime` = `b`.`atime` ;